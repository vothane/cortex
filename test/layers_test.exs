defmodule LayerTest do
  use ExUnit.Case
  doctest Layer

  test "............" do
    assert 1 == 1
  end
end

defmodule DenseTest do
  use ExUnit.Case
  doctest Layer

  test "............" do
    assert 1 == 1
  end
end
